# Enforce STRICT mTLS for ALL services in istio-demo namespace.
# Any plaintext traffic is rejected — every hop must present a valid cert.
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio-demo
spec:
  mtls:
    mode: STRICT
